kind: Service
apiVersion: v1
metadata:
  name: npm-service-canary
spec:
  type: NodePort
  selector:
    app: npm-service-schedule
    track: canary
  ports:
  - protocol: TCP
    port: 8080
    nodePort: 8081

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: npm-deployment-canary
  labels:
    app: npm-service-schedule
spec:
  replicas: $CANARY_REPLICAS
  selector:
    matchLabels:
      app: npm-service-schedule
      track: canary
  template:
    metadata:
      labels:
        app: npm-service-schedule
        track: canary
    spec:
      containers:
      - name: npm-service-schedule
        image: $DOCKER_IMAGE_NAME:$BUILD_NUMBER
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 15
          timeoutSeconds: 1
          periodSeconds: 10
        resources:
          requests:
            cpu: 200m
